const mainPrompt = require('./main-question');
const viewPrompt = require('./view-questions')
const updatePrompt = require('./update-question')

module.exports = {
    mainPrompt,
    viewPrompt,
    updatePrompt,
}